export interface svgsOptions {
    dir?: string; // svg目录path
    buildDir?: string; // build 后输出的目录path
    moduleId?: string;
    svgId?: string;
    iconPrefix?: string;
    rootDir: string;
    debug?: boolean;
}
